name: Notion → Blog Sync

# 자동화 트리거 설정
on:
  # 수동 실행 (GitHub Actions 탭에서)
  workflow_dispatch:
    inputs:
      limit:
        description: '최대 변환 개수 (비우면 전체)'
        required: false
        type: number
      dry_run:
        description: 'Dry-run 모드 (테스트)'
        required: false
        type: boolean
        default: false

  # 스케줄 실행 (매일 자정, KST 기준 오전 9시)
  # schedule:
  #   - cron: '0 0 * * *'  # UTC 00:00 = KST 09:00

  # Push 이벤트 (특정 파일 변경 시)
  # push:
  #   paths:
  #     - 'scripts/sync-all-notion.js'
  #     - '.github/workflows/notion-sync.yml'

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 전체 히스토리 가져오기 (git commit용)

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔍 Test Notion connection
        env:
          NOTION_API_TOKEN: ${{ secrets.NOTION_API_TOKEN }}
        run: node scripts/test-notion-sync.js

      - name: 🔄 Sync Notion pages
        env:
          NOTION_API_TOKEN: ${{ secrets.NOTION_API_TOKEN }}
        run: |
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            echo "🧪 Dry-run 모드로 실행"
            node scripts/sync-all-notion.js --dry-run ${{ inputs.limit && format('--limit {0}', inputs.limit) || '' }}
          else
            echo "📝 실제 변환 실행"
            node scripts/sync-all-notion.js ${{ inputs.limit && format('--limit {0}', inputs.limit) || '' }}
          fi

      - name: 🏗️ Build blog
        if: ${{ inputs.dry_run != true }}
        run: npm run build

      - name: 📊 Check for changes
        if: ${{ inputs.dry_run != true }}
        id: check_changes
        run: |
          if [[ -n $(git status --porcelain) ]]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "✅ 변경 사항 발견"
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "ℹ️ 변경 사항 없음"
          fi

      - name: 💾 Commit and push changes
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add src/content/blog/
          git commit -m "chore: Sync posts from Notion

          🤖 Generated with [Claude Code](https://claude.com/claude-code)

          Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
          git push

      - name: 📝 Summary
        if: always()
        run: |
          echo "## 🎯 Notion Sync 결과" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            echo "**모드**: 🧪 Dry-run (테스트)" >> $GITHUB_STEP_SUMMARY
          else
            echo "**모드**: 📝 실제 변환" >> $GITHUB_STEP_SUMMARY
          fi
          if [ -n "${{ inputs.limit }}" ]; then
            echo "**제한**: ${{ inputs.limit }}개" >> $GITHUB_STEP_SUMMARY
          else
            echo "**제한**: 전체" >> $GITHUB_STEP_SUMMARY
          fi
          if [ "${{ steps.check_changes.outputs.has_changes }}" = "true" ]; then
            echo "**결과**: ✅ 변경 사항 커밋됨" >> $GITHUB_STEP_SUMMARY
          else
            echo "**결과**: ℹ️ 변경 사항 없음" >> $GITHUB_STEP_SUMMARY
          fi

---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import CategoryHierarchy from '../components/CategoryHierarchy.astro';
import TagCloud from '../components/TagCloud.astro';
import { SITE_TITLE } from '../consts';
import { getLangFromUrl, t } from '../utils/i18n';

const lang = getLangFromUrl(Astro.url);
---

<!doctype html>
<html lang={lang}>
	<head>
		<BaseHead 
			title={`${lang === 'ko' ? '카테고리' : 'Categories'} - ${SITE_TITLE}`} 
			description={lang === 'ko' ? '블로그 콘텐츠를 카테고리와 태그별로 탐색하세요' : 'Explore blog content by categories and tags'} 
		/>
	</head>
	<body class="min-h-screen bg-gray-50 dark:bg-gray-900">
		<Header />
		<main id="main-content" class="max-w-6xl mx-auto px-4 py-8">
			<section>
				<div class="text-center mb-12">
					<h1 class="text-4xl font-bold text-gray-900 dark:text-gray-100 mb-4">
						{lang === 'ko' ? '카테고리 & 태그' : 'Categories & Tags'}
					</h1>
					<p class="text-xl text-gray-600 dark:text-gray-400 max-w-2xl mx-auto">
						{lang === 'ko' 
							? '관심 있는 주제를 선택해 관련 콘텐츠를 탐색하세요. 카테고리는 주제별로, 태그는 기술과 개념별로 구성되어 있습니다.'
							: 'Explore content by topics that interest you. Categories are organized by themes, while tags represent specific technologies and concepts.'
						}
					</p>
				</div>

				<!-- Category Hierarchy -->
				<CategoryHierarchy />

				<!-- Divider -->
				<div class="my-16">
					<div class="relative">
						<div class="absolute inset-0 flex items-center">
							<div class="w-full border-t border-gray-200 dark:border-gray-700"></div>
						</div>
						<div class="relative flex justify-center">
							<span class="bg-gray-50 dark:bg-gray-900 px-4 text-sm text-gray-500 dark:text-gray-400">
								{lang === 'ko' ? '또는' : 'OR'}
							</span>
						</div>
					</div>
				</div>

				<!-- Tag Cloud -->
				<div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 p-8">
					<TagCloud showTitle={true} maxTags={50} />
				</div>

				<!-- Call to Action -->
				<div class="text-center mt-12">
					<div class="bg-gradient-to-r from-primary-100 to-blue-100 dark:from-primary-900 dark:to-blue-900 rounded-xl p-8">
						<h3 class="text-2xl font-bold text-gray-900 dark:text-gray-100 mb-4">
							{lang === 'ko' ? '모든 글 보기' : 'Browse All Posts'}
						</h3>
						<p class="text-gray-600 dark:text-gray-400 mb-6">
							{lang === 'ko' 
								? '카테고리와 상관없이 모든 블로그 글을 시간순으로 확인하세요'
								: 'View all blog posts chronologically, regardless of category'
							}
						</p>
						<a 
							href="/blog"
							class="inline-flex items-center gap-2 bg-primary-600 hover:bg-primary-700 text-white font-medium px-6 py-3 rounded-lg transition-colors"
						>
							{lang === 'ko' ? '블로그로 이동' : 'Go to Blog'}
							<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
								<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
							</svg>
						</a>
					</div>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>